name: aapda_rakshak
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  flutter_map: ^6.1.0
  latlong2: ^0.9.0

  firebase_core: ^3.3.0
  cloud_firestore: ^5.4.4
  # geolocator: ^14.0.2
  google_maps_flutter: ^2.14.0
  google_maps_flutter_web: any # <-- ADD THIS LINE

  cupertino_icons: ^1.0.8
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
